<%= form_with(model: @student_response, local: true, html: {multipart: true}) do |form| %>
  <% if @student_response.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@student_response.errors.count, "error") %> prohibited this student response from being saved:</h2>

      <ul>
        <% @student_response.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :task_id, value: params[:task_id] %>

  <div class="mb-3">
    <%= form.label :file, class: "form-label" %>
    <%= form.file_field :file, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
